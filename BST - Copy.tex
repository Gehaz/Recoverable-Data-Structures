\section{BST}

\DontPrintSemicolon
\SetKw{CAS}{CAS}
\SetKw{True}{true}
\SetKw{False}{false}
\SetKw{Int}{int}

\newcommand{\clean}{C\scriptsize LEAN \normalsize}
\newcommand{\deleteflag}{DF\scriptsize LAG \normalsize}
\newcommand{\insertflag}{IF\scriptsize LAG \normalsize}
\newcommand{\mk}{M\scriptsize ARK \normalsize}
\newcommand{\Info}{Info}
\newcommand{\IInfo}{IInfo}
\newcommand{\DInfo}{DInfo}
\newcommand{\NULL}{\mbox{$\perp$}}
\newcommand{\Key}{\mbox{Key}}


\newcommand{\func}[1]{\mbox{\sc #1}}

\newcommand{\p}{\hspace*{7mm}}
\let\oldnl\nl% Store \nl in \oldnl
\newcommand{\nonl}{\renewcommand{\nl}{\let\nl\oldnl}}% Remove line number for one line

\makeatletter
\newcommand{\algorithmstyle}[1]{\renewcommand{\algocf@style}{#1}}
\makeatother

\algorithmstyle{plain}

\begin{figure}

	\begin{algorithm}[H]
		
		type Update \{ \tcp*{stored in one CAS word}
			\p	\{\clean,\deleteflag,\insertflag,\mk\} state \;
			\p	Info *{\it info} \;
		\} \;
		
		type Internal \{ \tcp*{subtype of Node}
			\p $Key \cup \{\infty_1,\infty_2\}$ $key$ \;
			\p Update $update$ \;
			\p Node *$left$, *$right$ \;
		\} \;
		
		type Leaf \{ \tcp*{subtype of Node}
			\p $Key \cup \{\infty_1,\infty_2\}$  $key$ \;
		\} \;

		type \IInfo\ \{ \tcp*{subtype of \Info}
			\p Internal *$p$, *$newInternal$ \;
			\p Leaf *$l$ \;
		\} \;
		
		type \DInfo \{ \tcp*{subtype of \Info}
			\p Internal *$gp$, *$p$ \;
			\p Leaf *$l$ \;
			\p Update $pupdate$ \;
		\} \;   	
		
		\nonl $\vartriangleright$ Initialization: \;
		shared Internal *$Root$ := pointer to new Internal node \;
			\nonl \p with $key$ field $\infty_2$, $update$ field $\langle \clean,\NULL\rangle$, and \;
			\nonl \p pointers to new Leaf nodes with keys $\infty_1$ and \;
			\nonl \p $\infty_2$, respectively, as $left$ and $right$ fields. 
	\end{algorithm}
	
	\caption{\label{code1}Type definitions and initialization.}
\end{figure}


\begin{figure*}
	
	\func{aglo}
	
	\begin{procedure}[H]
		\caption{Search ($\Key\ k$) : $\langle \mbox{Internal*}, \mbox{Internal*}, \mbox{Leaf*}, \mbox{Update}, \mbox{Update}\rangle$}
		
	\end{procedure}
	
	
	
	
\end{figure*}






 